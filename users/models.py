from django.db import models
from django.contrib.auth.models import User


# No custom models are required yet.
# Django's built-in User model handles:
# username, password, email, login, logout, etc.